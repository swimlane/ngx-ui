import { InputHintDirective } from './input-hint.directive';

describe('InputHintDirective', () => {
  let directive: InputHintDirective;

  beforeEach(() => {
    directive = new InputHintDirective();
  });

  it('should be defined', () => {
    expect(directive).toBeDefined();
  });
});
