You can apply **Glow** classes to achieve the glow effect: `.glow-{color-name}-{weight}`.

Alternatively, you can also use `@mixin glow()` to apply glow effect using any color:

```scss
.custom-glow-class {
  @include glow(#bd4242);
}
```
