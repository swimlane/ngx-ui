The default duration of the `ngx-long-press-button` is 3 seconds. after which `longPress` output will emit.

```html
<ngx-long-press-button (longPress)="onClick('button click')"></ngx-long-press-button>
```
