import { IfTabActiveDirective } from './if-tab-active.directive';

describe('IfTabActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IfTabActiveDirective();
    expect(directive).toBeTruthy();
  });
});
