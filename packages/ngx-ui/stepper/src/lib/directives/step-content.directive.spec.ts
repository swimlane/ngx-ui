import { StepContentDirective } from './step-content.directive';

describe('StepContentDirective', () => {
  it('should create an instance', () => {
    const directive = new StepContentDirective();
    expect(directive).toBeTruthy();
  });
});
