import { InputPrefixDirective } from './input-prefix.directive';

describe('InputPrefixDirective', () => {
  it('should create an instance', () => {
    const directive = new InputPrefixDirective();
    expect(directive).toBeTruthy();
  });
});
