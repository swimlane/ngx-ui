import { InputSuffixDirective } from './input-suffix.directive';

describe('InputSuffixDirective', () => {
  it('should create an instance', () => {
    const directive = new InputSuffixDirective();
    expect(directive).toBeTruthy();
  });
});
