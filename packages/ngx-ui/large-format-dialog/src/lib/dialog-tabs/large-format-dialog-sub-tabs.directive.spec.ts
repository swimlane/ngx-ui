import { LargeFormatDialogSubTabsDirective } from './large-format-dialog-sub-tabs.directive';

describe('LargeFormatDialogSubTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new LargeFormatDialogSubTabsDirective();
    expect(directive).toBeTruthy();
  });
});
