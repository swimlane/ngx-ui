import { LargeFormatDialogTabsDirective } from './large-format-dialog-tabs.directive';

describe('LargeFormatDialogTabsDirective', () => {
  it('should create an instance', () => {
    const directive = new LargeFormatDialogTabsDirective();
    expect(directive).toBeTruthy();
  });
});
