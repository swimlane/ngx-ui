import { LargeFormatDialogSubStepperDirective } from './large-format-dialog-sub-stepper.directive';

describe('LargeFormatDialogSubStepperDirective', () => {
  it('should create an instance', () => {
    const directive = new LargeFormatDialogSubStepperDirective();
    expect(directive).toBeTruthy();
  });
});
