import { LargeFormatDialogStepperDirective } from './large-format-dialog-stepper.directive';

describe('LargeFormatDialogStepperDirective', () => {
  it('should create an instance', () => {
    const directive = new LargeFormatDialogStepperDirective();
    expect(directive).toBeTruthy();
  });
});
