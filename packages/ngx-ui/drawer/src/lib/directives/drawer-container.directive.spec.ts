import { DrawerContainerDirective } from './drawer-container.directive';

describe('DrawerContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new DrawerContainerDirective();
    expect(directive).toBeTruthy();
  });
});
