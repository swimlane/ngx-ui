import { IntersectDirective } from './intersect.directive';

describe('IntersectDirective', () => {
  it('should create an instance', () => {
    const directive = new IntersectDirective();
    expect(directive).toBeTruthy();
  });
});
