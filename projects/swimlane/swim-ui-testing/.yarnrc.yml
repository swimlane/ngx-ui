# Nested project: allow updating yarn.lock when the shell has CI=true (immutable default).
# CI uses `yarn install --immutable`, which still requires a committed lockfile.
enableImmutableInstalls: false
