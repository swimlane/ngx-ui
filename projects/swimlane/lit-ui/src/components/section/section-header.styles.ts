import { css } from 'lit';

/**
 * Section header is a simple wrapper; layout is controlled by the parent swim-section.
 */
export const sectionHeaderStyles = css`
  :host {
    display: contents;
  }
`;
