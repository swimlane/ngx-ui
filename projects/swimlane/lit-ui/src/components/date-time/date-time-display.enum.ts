/**
 * Display mode for date/time formatting.
 * Mirrors ngx-ui DATE_DISPLAY_TYPES.
 */
export enum DateDisplayType {
  HUMAN = 'human',
  TIMEZONE = 'timezone',
  LOCAL = 'local',
  CUSTOM = 'custom'
}
