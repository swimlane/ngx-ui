/**
 * Font family name used by lit-ui icon component. The host application must
 * load the icon font (e.g. via @font-face) with this family name so that
 * <swim-icon> glyphs render. Using a unique name avoids conflicts with the
 * host's own icon font version or glyph mappings.
 */
export const SWIM_ICON_FONT_FAMILY = 'swim-lit-icon';
